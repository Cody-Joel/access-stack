# Python
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Secrets — NEVER commit these
.env
.env.*
!.env.example
*.key
*.pem
secrets*
credentials*

# Private conversation data
sfm_conversations.json

# Logs / DBs / caches
*.log
*.db
*.sqlite
*.sqlite3

# OS / editor / build
.DS_Store
.idea/
.vscode/
dist/
build/

# .NET / ONNX build artifacts (large binaries — never commit)
ONNX/bin/
ONNX/obj/
*.dll
*.so
*.dylib
*.lib
*.aar
*.xcframework.zip

# Results data (real run outputs — keep local only)
SpecStack_Results/
*/results/*.json
!*/results/.gitkeep

# Personal application materials — keep private, do not publish
/application/
